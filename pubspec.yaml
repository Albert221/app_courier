name: app_courier
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_chooser: 0.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  lint: ^1.3.0

flutter:
  uses-material-design: true
